# Screenshots

Drop PNG files in this folder once the frontend is rebuilt and deployed (e.g. `mobile-ui.png`,
`ci-pipeline.png`, `tests-passing.png`) and reference them from the root `README.md`.

Recommended width: ~1200px. PNG preferred.
